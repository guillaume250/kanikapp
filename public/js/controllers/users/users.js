angular.module("usersCtrl", []).controller('usersCtrl',function($scope){
  $scope.title = "Features under development"
  $scope.contents = [
    "User management system",
    "User access control system"
  ]
});
